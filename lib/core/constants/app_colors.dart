import 'package:flutter/material.dart';

/// Biggopti Design Tokens - Organic, Modern & Elegant Bangla Palette
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF0A4D3C);        // Deep Forest Green
  static const Color primaryDark = Color(0xFF062F25);    // Dark Forest Slate
  static const Color secondary = Color(0xFF9EC5B2);      // Sage Green / Mint
  static const Color accent = Color(0xFFFF8B8B);         // Coral Pink
  static const Color accentLavender = Color(0xFFB1A7F2); // Soft Lavender
  
  static const Color surface = Color(0xFFFFFFFF);        // Pure White Cards
  static const Color background = Color(0xFFF4F6F4);     // Soft Slate Background
  
  static const Color textPrimary = Color(0xFF1A2320);    // Dark Typography
  static const Color textSecondary = Color(0xFF5A6B63);  // Muted Body Text
  static const Color textLight = Color(0xFF8A9A92);      // Light Subtitle Text
  
  // Semantic Colors
  static const Color success = Color(0xFF1B873F);        // Verified / Eligible
  static const Color warning = Color(0xFFFFB300);        // Approaching Deadline
  static const Color danger = Color(0xFFD0021B);         // Expired / Ineligible
  
  // Border & Divider Colors
  static const Color border = Color(0xFFE2E8E4);
  static const Color chipBackground = Color(0xFFEAEFEA);
}
